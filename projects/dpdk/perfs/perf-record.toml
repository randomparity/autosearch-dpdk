# Shared profiler settings (tracked in git).
# System-specific overrides: perf-record.local.toml (gitignored).

[profiling]
# Capture perf profiles during measurement window
enabled = false

# Sampling frequency in Hz (99 avoids aliasing with timer interrupts)
frequency = 99

# Run perf commands with sudo
sudo = true
