# Shared test settings (tracked in git).
# System-specific overrides: bench-serving.local.toml (gitignored).

[bench]
num_prompts = 100
dataset_name = "random"
random_input_len = 512
random_output_len = 256
max_concurrency = 64
request_rate = "inf"
